{
  "name": "micro-action",
  "version": "1.1.1",
  "private": true,
  "description": "First-party OIDC deployment action for micro.do",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/index.js && node --check dist/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  }
}
