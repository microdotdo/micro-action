name: Deploy production fixture

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-24.04
    environment: production
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - id: deploy
        uses: ./
        with:
          path: test/fixture
          environment: production
      - name: Verify deployment outputs
        env:
          MICRO_URL: ${{ steps.deploy.outputs.url }}
          MICRO_PROJECT_ID: ${{ steps.deploy.outputs.project-id }}
          MICRO_DEPLOYMENT_ID: ${{ steps.deploy.outputs.deployment-id }}
          MICRO_VERIFICATION_STATUS: ${{ steps.deploy.outputs.verification-status }}
        run: |
          test "$MICRO_URL" = "https://action-e2e-microdotdo.micro.do"
          test -n "$MICRO_PROJECT_ID"
          test -n "$MICRO_DEPLOYMENT_ID"
          test "${MICRO_VERIFICATION_STATUS%%:*}" = "reachable"
